# Known gaps during Phase 2C-C4 planning acceptance

## Closed by accepted C3

- registry-selected active generation resolution;
- full fail-closed identity/seal/hash matrix;
- stale pointer detection;
- no fallback or mutation;
- deterministic reopen/reload;
- actual-Chrome REQ-OBS-001 and sanitized diagnostics.

## Defined by the C4 planning candidate, not yet accepted

- exact artifact/source/backup/registry/generation/attempt/authorization identity contract;
- authorization consumption before source open;
- strict bootstrap-empty target and collision gates;
- checkpoint import/reopen/hash/seal/promotion/resolver sequence;
- failure/reload matrix without automatic resume/retry/cleanup;
- separate rollback authorization;
- package inventory, modes, provenance and offline inspection;
- planning/implementation/package/execution/migration acceptance separation.

## Still open after planning acceptance

- C4 implementation on sanitized fixtures;
- real filesystem backup implementation and crash proof;
- exact package builder/manifest/authorization schema;
- target-Mac package verification;
- exact-source execution and actual migration/activation/rollback;
- production REQ-OBS-001 evidence;
- semantic quality, multi-order stability and real-data safety.

Exact SQLite/private backup access, production namespace, actual migration, model/network calls and personal data remain prohibited.
