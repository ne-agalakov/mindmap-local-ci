# Work boundary after Phase 2C-C4 planning acceptance

## Allowed

Only a separate C4 implementation issue on isolated sanitized fixtures: runner/state machine, detached fixture authorization, fixture backup, collision gates, import/reopen/hash/seal/promotion/C3 resolver, interruption/reload, separate rollback authorization, Chrome/macOS proof and sanitized diagnostics.

## Prohibited

Exact SQLite/private backup access; B1b repeat; filesystem source search; target-Mac production registry/generation; exact execution package/authorization; actual migration/promotion/rollback; automatic resume/retry/cleanup; model/network calls; personal data.
