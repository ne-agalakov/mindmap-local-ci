# Notes

The original macOS uploader completed and pushed the implementation, but `rg` was unavailable. That specific inline scan is therefore not treated as evidence. The exact mapping source files were independently rechecked with an equivalent pattern scan and produced zero findings. The missing local receipt is replaced by reconstructed provenance records; it is not represented as the original receipt.
